var button = document.getElementById('bar');

// Add a click event listener to the button
button.addEventListener('click', function () {
  'use strict';
  // Display the letter 'A' on the console
  console.log('A');
});